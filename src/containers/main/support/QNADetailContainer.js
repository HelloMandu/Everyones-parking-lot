import React from 'react';
/* Library */
import { useLocation} from 'react-router-dom';

const QNADetailContainer = ({qna_id}) => {
    const location = useLocation();
    return (
        <div>
            1:1 문의 상세보기
        </div>
    );
};

export default QNADetailContainer;