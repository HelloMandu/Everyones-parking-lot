import React from 'react';
/* Library */

const PaymentContainer = () => {

    // + 적용 쿠폰 선택, 결제 수단 선택

    return (
        <div>
            결제
        </div>
    );
};

export default PaymentContainer;